<% include ../partials/header %>

<div class="container">
    <header class="jumbotron">
        <div class="container">
            <h1>Welcome to YelpHütte</h1>
            <p>YelpHütte is your No.1 choice when it comes to accomodation in alpine region. <em>Hüttes</em> [hut, cabin] are
            popular among mountain enthusiasts and hikers alike. Whether you are looking for an overnight stay or 
            refreshments on your way to the summint they are great fun!</p>
            <p>Know some great hüttes that are not here?</p>
            <p><a class="btn btn-primary btn-large" href="campgrounds/new">Add new</a></p>
        </div>
    </header>
    
    <div class="row">
        <div class="col-lg-12">
            <h3> Our most popular huttes</h3>
        </div>
    </div>
    <div class="row text-center" style="display:flex; flex-wrap: wrap;">
        <% campgrounds.forEach(function(campground){ %>
        <div class="col-md-3 col-sm-6">
            <div class="thumbnail">
                <% var img = campground.uploadPath || campground.image %>
                <img class="img-fullsize" src="<%= img %>">
                <div class="caption">
                    <h4><%= campground.name %></h4>
                    <p>
                        <a href="/campgrounds/<%= campground._id %>" class="btn btn-primary">More Info</a>
                    </p>
                </div>
            </div>
        </div>
    <% }); %>
    </div>
    
</div>
<% include ../partials/footer %>